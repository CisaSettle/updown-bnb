{
  "name": "@updown/keeper",
  "version": "1.0.0",
  "private": true,
  "description": "UpDown Protocol keeper \u2014 drives executeRound() (and testnet price relays) for every deployed market on BNB Smart Chain.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "main": "dist/index.js",
  "bin": {
    "updown-keeper": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "dev": "node --watch src/index.ts",
    "test": "tsc -p tsconfig.test.json --noEmit && vitest run",
    "test:watch": "vitest",
    "test:unit": "vitest run"
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
