# UpDown — agent instructions

The working agreement for this repository is [`AGENTS.md`](AGENTS.md). It is deliberately one file
for both vendors: Codex reads `AGENTS.md` by convention, Claude Code reads this one, and pointing
here rather than copying is what stops the two from drifting apart.

**Read `AGENTS.md` now, and run `node scripts/agent-ledger.mjs preflight` before touching
anything** — another session may already hold the workstream you are about to start.
